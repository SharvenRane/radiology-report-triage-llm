version: "3.9"

services:
  triage:
    build: .
    container_name: radiology-triage
    volumes:
      - .:/app
    command: bash -c "python src/embed.py && python src/classify.py && python src/visualize.py"
